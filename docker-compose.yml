services:
  antigravity-mail:
    build: .
    container_name: antigravity-mail
    ports:
      - "8085:8000"
    volumes:
      # Persistent SQLite database volume
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
